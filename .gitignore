option-c-unikernel/target/
# Receipts in out/ are ephemeral; those under artifacts/ are published records
out/*.receipt.json
artifacts/*.raw
artifacts/*.raw.gz
artifacts/v0.1.0/
out/*.raw
out/*.raw.gz
